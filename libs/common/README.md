# common

A TS Library containing shared code, across futurepass apps. (not to be used by SDKs)

## Building

Run `nx build common` to build the library.

## Running unit tests

Run `nx test common` to execute the unit tests via [Jest](https://jestjs.io).
