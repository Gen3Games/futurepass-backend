// eslint-disable-next-line import/export -- for some reason if it's not double exported, it's not working
export * from '../ethContracts'
// eslint-disable-next-line import/export -- for some reason if it's not double exported, it's not working
export * from '../ethContracts/Bridge'
