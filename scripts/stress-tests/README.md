# Instructions

1. Replace the value of `_vercel_jwt` with a valid JWT token to access the dashboard
2. Install `k6` locally
3. Update the options accordingly(VUs, iterations, duration).
4. Run ` k6 run stress_test.js` 
5. Activate debug mode with `k6 run stress_test.js --http-debug`
